apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "snacker-tracker-ui.fullname" . }}-test-connection"
  labels:
{{ include "snacker-tracker-ui.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "snacker-tracker-ui.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
